<div class="images">
  <% @images.each do |img| %>
    <div class="overlay">
      <div class="top-overlay">
        <%= link_to "delete", img, method: :delete, data: { confirm: "After deleting this image won't be possible restore it, Are you sure to delete it?" } %>
      </div>
      <div class="like">
        <%= button_to "Like", "/like/#{img.id}", method: :post %>
      </div>
    </div>
    <div class="image_<%=img.title%>">
      <div class="image-photo">
        <%= image_tag img.photo, width: 200 if img.photo.attached? %>
      </div>
      <div class="image-info">
        <div class="image-title">
          <%= img.title %>
        </div>
        <div class="image-location">
          <%= img.location %>
        </div>
      </div>
      <div class="image-tags">
        <div class="tag">
          <% img.tags.each do |t| %>
            <span id="<%= t.tag_name.delete(' ') %>_tag"><%= t.tag_name %></span>
           <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>
